{
  "name": "bible-api",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon --watch src --exec \"tsx src/index.ts\"",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma-validate": "npx prisma validate",
    "prisma-format": "npx prisma format",
    "prisma-generate": "npx prisma generate",
    "prisma-prepare": "npm run prisma-validate && npm run prisma-format && npm run prisma-generate",
    "prisma-migrate": "npx prisma migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.15.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "dotenv": "^17.2.1"
  }
}
